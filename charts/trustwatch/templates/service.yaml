apiVersion: v1
kind: Service
metadata:
  name: {{ include "trustwatch.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "trustwatch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "trustwatch.selectorLabels" . | nindent 4 }}
